import { ReferenciaPipe } from './referencia.pipe';

describe('ReferenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
